<div>
  <ul>
    <li><%= link("Favourites", to: favourite_path(@conn, :show)) %></li>
    <li><%= link("Qualifiers", to: qualified_path(@conn, :show)) %></li>
    <li><%= link("Predictions", to: prediction_path(@conn, :index)) %></li>
  </ul>
</div>
